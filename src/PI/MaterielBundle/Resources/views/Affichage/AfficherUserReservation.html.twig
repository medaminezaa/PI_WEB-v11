{% extends "PIBaseBundle::layout.html.twig" %}


    {% block content %}
 {% for flashMessage in app.session.flashbag.get('notice') %}

     <div class="alert alert-success">
         {{ flashMessage }}
     </div>

 {% endfor %}
        <h4 class="page-head-line">Vos Reservations</h4>

        <div class="row">
        <div class="col-md-12">

        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                <tr>

                    <th>Nom Materiel Reservé</th>
                    <th>Date Reservation</th>
                    <th>Quantite</th>
                    <th>Prix de la commande</th>






                </tr>
                </thead>
                <tbody>


                {% for Res in Reservation %}
                    <tr>
                        <td>{{ Res.idmateriel.nom}}</td>
                        <td>{{ Res.dateres|date}}</td>
                        <td>{{ Res.quantite }}</td>
                        <td>{{ Res.prix}} DT</td>
                    </tr>
                {% endfor %}



                </tbody>
            </table>
        </div>










        {{ knp_pagination_render(Reservation) }}




    {% endblock %}

{% block testimonails %}

            {% endblock %}